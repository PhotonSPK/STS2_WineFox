using MegaCrit.Sts2.addons.mega_text;

namespace STS2_WineFox.Scripts
{
    // Local wrapper so the counter scene can bind a mod-local script in editor.
    public partial class WineFoxCounterLabel : MegaLabel
    {
    }
}
